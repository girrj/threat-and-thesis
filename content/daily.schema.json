{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://threat-and-thesis.local/daily.schema.json",
  "title": "Threat & Thesis category rankings",
  "type": "object",
  "additionalProperties": false,
  "required": ["date", "generatedAt", "rankings"],
  "properties": {
    "date": { "type": "string", "format": "date" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "selectionMode": { "enum": ["new-only"] },
    "rankings": {
      "type": "object",
      "additionalProperties": false,
      "required": ["security", "ai-security", "security-paper", "ai-paper", "technology"],
      "properties": {
        "security": { "$ref": "#/$defs/rankingList" },
        "ai-security": { "$ref": "#/$defs/rankingList" },
        "security-paper": { "$ref": "#/$defs/rankingList" },
        "ai-paper": { "$ref": "#/$defs/rankingList" },
        "technology": { "$ref": "#/$defs/rankingList" }
      }
    }
  },
  "$defs": {
    "rankingList": {
      "type": "array",
      "maxItems": 10,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["rank", "itemId", "previousRank", "status", "reason"],
        "properties": {
          "rank": { "type": "integer", "minimum": 1 },
          "itemId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9.-]*$" },
          "previousRank": {
            "anyOf": [
              { "type": "integer", "minimum": 1 },
              { "type": "null" }
            ]
          },
          "status": { "enum": ["new", "up", "down", "same", "returning"] },
          "reason": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
